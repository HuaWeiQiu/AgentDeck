<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Isolated Secure-channel chat-performance surface. Not a launcher
             and never packaged into Lab. -->
        <activity
            android:name=".ui.chat.ChatPerformanceBenchmarkActivity"
            android:exported="true"
            android:theme="@style/Theme.AgentDeck" />
    </application>
</manifest>
