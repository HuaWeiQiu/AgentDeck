# AgentDeck `0.2.0-beta.10`

安全通道预发布包（`secure`）。Lab 见 `v0.2.0-beta.10-lab.md`。

## 产物

| 文件 | applicationId |
| --- | --- |
| `app-secure-arm64-v8a-beta.apk` | `com.agentdeck.app.debug` |
| `app-secure-x86_64-beta.apk` | `com.agentdeck.app.debug` |

版本：`versionName=0.2.0-beta.10` · `versionCode=15`

## 本版要点

- 新建/编辑会话可直接写人设，不必先开高级选项。
- 准备页用人话说明下载和换线路。
- 设置可导出会话名称与角色；不含密钥和聊天正文。
- 首次打开提示：记录只在这台手机。

## Secure 边界

- 不含无障碍屏幕代理，也不含本地 MCP。
- 备份文件不含 API Key、Bearer、capability。

## 自动验证

- Secure JVM：352 项通过。
- Secure APK 扫描不含 `LabAccessibilityService`。

## 已知边界

- 正式签名和去掉 `.debug` 包名仍未做。
- Runtime 仍是一份 Codex rootfs；体积拆分见下一轮计划。
