# AgentDeck Lab `0.2.0-beta.8-lab`

高权限实验通道候选产物，不是日常默认包；本轮按要求不做 Lab 真机测试。

## 产物

- `app-lab-arm64-v8a-beta.apk` → `com.agentdeck.app.lab.debug`
- `app-lab-x86_64-beta.apk`

版本：`0.2.0-beta.8-lab` · `versionCode=13`

## 相对 Secure 增加

- 本地 stdio MCP：受限绝对命令路径与结构化参数。
- 继续保留原始 TOML MCP，适合开发和兼容实验。
- `EXTENSION_MAX_LEVEL=4`；本地命令注入器仅存在于 `src/lab`。

Lab 同时包含 Secure 的 Skill、远程 HTTPS MCP、Keystore Bearer、每对话选择、工具 allowlist 和详情编辑能力。

## 自动验证

- Lab JVM：319 项通过。
- `lintLabBeta`、R8、ARM64/x86_64 拆分构建通过。
- 两个 Lab APK 均确认包含 `LabLocalMcpRuntimeAdapter`。

## 未验证

- 未安装到真机，不声明本地 MCP 在具体 OEM/PRoot 上已经验收。
- 本地 MCP 不提供 Node/npm Runtime；只有当前 Ubuntu Runtime 中实际存在的固定命令可用。
