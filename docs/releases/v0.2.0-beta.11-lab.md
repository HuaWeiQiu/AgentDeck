# AgentDeck Lab `0.2.0-beta.11-lab`

实验通道预发布包（`lab`）。Secure 见 `v0.2.0-beta.11.md`。

## 产物

| 文件 | applicationId |
| --- | --- |
| `app-lab-arm64-v8a-beta.apk` | `com.agentdeck.app.lab.debug` |
| `app-lab-x86_64-beta.apk` | `com.agentdeck.app.lab.debug` |

版本：`0.2.0-beta.11-lab` · `versionCode=16`

## 相对 Secure

- 含 Lab 屏幕 Agent 等实验入口（默认关闭）。
- 会话模式（轻聊 / 开发）与 Secure 相同；狂暴能力仍只在 Lab 包。

## 注意

- 测试签名，非正式生产签名。
