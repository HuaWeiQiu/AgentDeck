# AgentDeck Lab `0.2.0-beta.9-lab`

高权限实验通道候选产物，不是日常默认包；本轮按要求不做 Lab 真机性能测试。

## 产物

- `app-lab-arm64-v8a-beta.apk` → `com.agentdeck.app.lab.debug`
- `app-lab-x86_64-beta.apk`

版本：`0.2.0-beta.9-lab` · `versionCode=14`

## 相对 Secure 增加

- 本地 stdio MCP：受限绝对命令路径与结构化参数。
- 继续保留原始 TOML MCP，适合开发和兼容实验。
- `EXTENSION_MAX_LEVEL=4`；本地命令注入器仅存在于 `src/lab`。

Lab 与 Secure 共享同一聊天栈，因此本版聊天性能改造（有界窗口、投影、trim、排版修复、下载看门狗）两边同时生效。

## 自动验证

- Lab JVM：343 项通过。
- `lintLabBeta` 与 Lab debug 拆分构建通过。

## 未验证

- 未在真机采集 Lab 性能数字；官方门槛只认 Secure Beta。
- 本地 MCP 不提供 Node/npm Runtime；只有当前 Ubuntu Runtime 中实际存在的固定命令可用。
